{
  "name": "rafaelelyah",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "node deploy.js",
    "clean": "rm -rf docs",
    "version": "node deploy.js version-only"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "sass": "^1.90.0",
    "vite": "^7.1.0"
  }
}